
import React from 'react';
import ReactDOM from 'react-dom';

function Saudacao(props) {
  return <h1>Bom dia, {props.nome}</h1>;
}

const elemento = <Saudacao nome="Priscila" />;
ReactDOM.render(
  elemento,
  document.getElementById('root')
);
